import { Injectable } from '@angular/core';
@Injectable()
export class CommonService {
    // private setLoader = false;
    showLoader = false;
    constructor() {
    }
    // get showLoader(): boolean { return this.setLoader; }
    // set showLoader(name) { this.setLoader = name; }
}
